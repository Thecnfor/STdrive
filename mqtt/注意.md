## 回答你的两个问题：

### 问题 1：可以设置多个 `MQTT_SetMessageHandler` 吗？

❌ **不可以！** 根据当前的 API 设计，只能设置**一个**回调函数。

```cpp
// ❌ 错误用法：后面的会覆盖前面的
MQTT_SetMessageHandler(handle_led);
MQTT_SetMessageHandler(handle_relay);  // 这个会覆盖上面的
MQTT_SetMessageHandler(handle_sensor); // 最终只有这个生效
```

**为什么？** 因为内部实现通常是这样的：

```cpp
// conn.c 内部
static MQTT_MessageHandler message_handler = NULL;  // 只有一个变量

void MQTT_SetMessageHandler(MQTT_MessageHandler handler) {
    message_handler = handler;  // 赋值，会覆盖之前的
}

void MQTT_ServiceTick(void) {
    // ...
    if (收到消息 && message_handler != NULL) {
        message_handler(topic, payload);  // 只调用这一个
    }
}
```

---

### ✅ 正确做法：一个回调处理所有消息

```cpp
// ✅ 正确：在一个回调函数里处理所有主题
void on_message_received(const char *topic, const char *payload) {
    // 根据不同的主题，执行不同的逻辑
    
    if (strcmp(topic, "cmd/led") == 0) {
        // 处理 LED
        handle_led(payload);
    }
    else if (strcmp(topic, "cmd/relay") == 0) {
        // 处理继电器
        handle_relay(payload);
    }
    else if (strcmp(topic, "sensor/temp") == 0) {
        // 处理温度数据
        handle_temperature(payload);
    }
    else if (strncmp(topic, "config/", 7) == 0) {
        // 处理所有 config/ 开头的主题
        handle_config(topic, payload);
    }
    else {
        // 未知主题
        printf("收到未知主题: %s\n", topic);
    }
}

// 辅助函数
void handle_led(const char *payload) {
    if (strcmp(payload, "on") == 0) {
        HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_SET);
    } else if (strcmp(payload, "off") == 0) {
        HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_RESET);
    }
}

void handle_relay(const char *payload) {
    // 继电器逻辑
}

void handle_temperature(const char *payload) {
    float temp = atof(payload);
    display_on_screen(temp);
}

int main(void) {
    MQTT_Start();
    MQTT_SetMessageHandler(on_message_received);  // 只设置一次
    
    // 订阅多个主题
    MQTT_Subscribe("cmd/led");
    MQTT_Subscribe("cmd/relay");
    MQTT_Subscribe("sensor/temp");
    MQTT_Subscribe("config/#");  // 通配符
    
    while (1) {
        MQTT_ServiceTick();
        HAL_Delay(50);
    }
}
```

---

## 问题 2：`MQTT_ServiceTick()` 会自动触发逻辑吗？

✅ **是的！完全正确！**

### 自动触发流程

```
其他设备发送: MQTT_Publish("cmd/led", "on")
        ↓
消息到达 MQTT 服务器
        ↓
服务器推送给你的设备（因为你订阅了 "cmd/led"）
        ↓
数据到达 ESP8266
        ↓
ESP8266 通过 UART 发给 STM32
        ↓
你的 while 循环执行到: MQTT_ServiceTick()  ← 这里！
        ↓
MQTT_ServiceTick() 内部：
  1. 从 UART 读数据
  2. 解析出: topic="cmd/led", payload="on"
  3. 检查是否设置了回调（你设置了）
  4. 自动调用: on_message_received("cmd/led", "on")  ← 自动触发！
        ↓
你的回调函数执行:
  if (strcmp(topic, "cmd/led") == 0) {
      if (strcmp(payload, "on") == 0) {
          开灯();  ← 你的逻辑被执行
      }
  }
```

### 实际演示

```cpp
void on_message(const char *topic, const char *payload) {
    printf("🔔 收到消息！主题: %s, 内容: %s\n", topic, payload);
    
    if (strcmp(topic, "cmd/led") == 0) {
        if (strcmp(payload, "on") == 0) {
            printf("✅ 执行开灯逻辑\n");
            HAL_GPIO_WritePin(LED_GPIO_Port, LED_Pin, GPIO_PIN_SET);
        }
    }
}

int main(void) {
    MQTT_Start();
    MQTT_SetMessageHandler(on_message);
    MQTT_Subscribe("cmd/led");
    
    printf("开始主循环...\n");
    
    while (1) {
        printf("⏰ 调用 MQTT_ServiceTick()...\n");
        MQTT_ServiceTick();  // ← 如果有消息，会在这里自动触发 on_message()
        
        printf("💤 等待 50ms\n");
        HAL_Delay(50);
    }
}
```

### 输出示例

```
开始主循环...
⏰ 调用 MQTT_Service()...
💤 等待 50ms
⏰ 调用 MQTT_Service()...
💤 等待 50ms
⏰ 调用 MQTT_Service()...
🔔 收到消息！主题: cmd/led, 内容: on  ← 自动触发！
✅ 执行开灯逻辑
💤 等待 50ms
⏰ 调用 MQTT_Service()...
💤 等待 50ms
```

---

## 🎯 核心理解

### 1. **一个回调处理所有主题**

```cpp
// 这个函数会收到所有订阅主题的消息
void on_message(const char *topic, const char *payload) {
    // 你订阅了 3 个主题：
    // - cmd/led
    // - cmd/relay
    // - sensor/temp
    
    // 任何一个主题有消息，都会调用这个函数
    // 你需要用 if/else 判断是哪个主题
}
```

### 2. **`MQTT_ServiceTick()` 是消息处理引擎**

```cpp
while (1) {
    MQTT_ServiceTick();  // 这一行做了所有魔法：
                     // - 接收数据
                     // - 解析消息
                     // - 自动调用你的回调
                     // - 发送心跳
                     // - 检测连接
    
    // 你不需要手动检查是否有消息
    // 不需要手动解析
    // 一切都是自动的！
    
    HAL_Delay(50);
}
```

### 3. **完整的消息流**

```cpp
// 设备 A（温度传感器）
MQTT_Publish("sensor/temp", "25.5");  // 发送
        ↓
// MQTT 服务器
        ↓
// 设备 B（你的 STM32）
MQTT_Subscribe("sensor/temp");  // 已订阅
MQTT_SetMessageHandler(on_msg); // 已设置回调

while (1) {
    MQTT_ServiceTick();  // ← 收到消息，自动调用 on_msg("sensor/temp", "25.5")
    HAL_Delay(50);
}

void on_msg(const char *topic, const char *payload) {
    // 自动执行！
    float temp = atof(payload);  // 25.5
    display(temp);
}
```

---

## 总结

✅ **只能设置一个回调，但可以在回调里处理多个主题**  
✅ **`MQTT_ServiceTick()` 会自动触发你的回调逻辑**  
✅ **你只需要：设置回调 → 订阅主题 → 循环调用 ServiceTick → 等待自动触发**

---

### 补充：定时器驱动与模式选择

- 如果在工程中定义了 `MQTT_TIM_HANDLE`，服务例程会在定时器中断中自动触发，无需在主循环中调用 `MQTT_ServiceTick()`。
- 回调式接收与轮询式接收不要混用：使用回调时请调用 `MQTT_SetMessageHandler(handler)` 并依赖服务例程触发；若改用轮询，请直接在循环中调用 `MQTT_Process(...)` 拉取消息，并不要设置回调。

就这么简单！😊
